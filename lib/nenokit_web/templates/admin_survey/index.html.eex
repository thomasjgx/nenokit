<div class="card">
  <div class="card-header">Surveys</div>
  <div class="card-datatable table-response p-3">
    <div class="row align-items-center mr-3">
      <div class="col-sm-12">
        <a class="btn btn-primary float-right mb-3" href="<%= Routes.admin_survey_path(@conn, :new) %>">New Survey</a>
      </div>
    </div>
    <table class="datatables-simple table table-striped table bordered">
      <thead>
        <tr>
          <th width="5%">#</th>
          <th width="40%">Name</th>
          <th>Last Updated</th>
          <th>Created on</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
      <%= for survey <- @surveys do %>
        <tr>
          <td><%= survey.id %></td>
          <td><%= survey.name %></td>
          <td><%= get_days_ago(survey.updated_at) %></td>
          <td><%= survey.inserted_at %></td>
          <td>
            <%= link "Show", to: Routes.admin_survey_path(@conn, :show, survey), class: "btn btn-info btn-sm mr-2" %>
            <%= link "Edit", to: Routes.admin_survey_path(@conn, :edit, survey), class: "btn btn-primary btn-sm mr-2"%>
            <%= link "Delete", to: Routes.admin_survey_path(@conn, :delete, survey), method: :delete, class: "btn btn-danger btn-sm" %>
          </td>
        </tr>
      <% end %>
      </tbody>
    </table>
  </div>
</div>
