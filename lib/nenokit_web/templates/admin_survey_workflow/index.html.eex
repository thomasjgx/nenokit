<div class="card">
  <div class="card-header">Survey Workflows</div>
  <div class="card-datatable table-response p-3">
    <div class="row align-items-center mr-3">
      <div class="col-sm-12">
        <a class="btn btn-primary float-right mb-3" href="<%= Routes.admin_survey_workflow_path(@conn, :new) %>">New Workflow</a>
      </div>
    </div>
    <table class="datatables-simple table table-striped table bordered">
      <thead>
        <tr>
          <th width="5%">#</th>
          <th width="40%">Name</th>
          <th>Last Updated</th>
          <th>Created on</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
      <%= for workflow <- @workflows do %>
        <tr>
          <td><%= workflow.id %></td>
          <td><%= workflow.name %></td>
          <td><%= get_days_ago(workflow.updated_at) %></td>
          <td><%= workflow.inserted_at %></td>
          <td>
            <%= link "Show", to: Routes.admin_survey_workflow_path(@conn, :show, workflow), class: "btn btn-info btn-sm mr-2" %>
            <%= link "Edit", to: Routes.admin_survey_workflow_path(@conn, :edit, workflow), class: "btn btn-primary btn-sm mr-2"%>
            <%= link "Delete", to: Routes.admin_survey_workflow_path(@conn, :delete, workflow), method: :delete, class: "btn btn-danger btn-sm" %>
          </td>
        </tr>
      <% end %>
      </tbody>
    </table>
  </div>
</div>
